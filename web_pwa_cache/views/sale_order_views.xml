<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_order_form_pwa" model="ir.ui.view">
        <field name="name">sale.order.form.pwa</field>
        <field name="model">sale.order</field>
        <field name="arch" type="xml">
            <formPWA string="Sales Order" class="o_sale_order">
                <field name="state" invisible="1" />
                <group class="p-0 m-0">
                    <field name="partner_id" widget="res_partner_many2one" domain="[('customer','=',True)]" context="{'search_default_customer':1, 'show_vat': True}" options='{"always_reload": True}' />
                </group>
                <group class="p-0 m-0">
                    <field
                        name="order_line"
                        attrs="{'readonly': [('state', 'in', ('done','cancel'))]}"
                        nolabel="1"
                        widget="one2many_product_picker"
                        options="{'groups': [{'name': 'Desk', 'domain': [('name', 'ilike', '%desk%')], 'order': {'name': 'id', 'asc': true}}, {'name': 'Chairs', 'domain': [('name', 'ilike', '%chair%')]}]}"
                    >
                        <kanban>
                            <field name="name"/>
                            <field name="product_id" />
                            <field name="product_uom_qty" />
                            <field name="product_uom"/>
                            <field name="price_unit"/>
                            <field name="order_id"/>
                        </kanban>
                    </field>
                </group>
            </formPWA>
        </field>
    </record>

    <!-- Necessary to get the model fields definition by the service worker -->
    <record id="sale_action_order_lines" model="ir.actions.act_window">
        <field name="name">Sale order lines</field>
        <field name="res_model">sale.order.line</field>
    </record>

</odoo>
